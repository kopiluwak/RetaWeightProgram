# Floors, not exact pins, so pip pulls prebuilt wheels matching your Python
# version (avoids source-building pydantic-core, which needs Rust).
fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
pydantic>=2.11
pydantic-settings>=2.7
python-jose[cryptography]>=3.3
boto3>=1.35
email-validator>=2.2
python-multipart>=0.0.18
